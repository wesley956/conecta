# LG 2.9.8 — Pacote 3 (#298, #299, #300)

## #298

A auditoria visual está registrada em `LG-2.9.8-VISUAL-AUDIT.md`, com tokens exatos, telas, estados, diferenças B e gates físicos.

## #299

Configurações consolida:

- Atualizar conteúdo separado de Verificar atualização;
- perfil/lista/validade atuais;
- tecnologia e decodificação automática justificada no webOS;
- aspecto, buffer, reconexão e som;
- categorias Clássica/Painel lateral;
- suporte responsável;
- lista principal/reserva, saúde, contagens, sincronização, snapshot e failover;
- versão/canal Stable;
- cache reconstruível, dados locais e desvínculo com confirmações separadas.

## #300

O candidate esperado é `com.ronecaplaytv.app_1.1.0_all.ipk`. O CI deve produzir SHA-256, metadata e manifesto com `physicalApproval=false`, `parityApproved=false` e `stableEligible=false`.

O fechamento exige TV LG física, preenchimento da matriz e autorização explícita. Não existe aprovação automática por código.
